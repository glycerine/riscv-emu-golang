/*
 * Stub for riscv-tests util.h. The vendored dhrystone_main.c does
 * `#include "util.h"` to pull in verify / barrier helpers, but Dhrystone
 * itself does not use them. Leaving this empty keeps the include happy.
 */
#ifndef __UTIL_H
#define __UTIL_H
#endif
